// IMPLEMENTING A LINKED LIST
class _Node {
  constructor(value, next) {
    this.value = value;
    this.next = next;
  }
}

// OPERATIONS IN A LINKED LIST 
class LinkedList {
    constructor() {
        this.head = null;
    }